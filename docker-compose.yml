services:
  catalogdb:
    image: postgres
  basketdb:
    image: postgres

  catalog.api:
    image: catalog.api
    build:
      context: .
      dockerfile: src/Services/Catalog.Api/Dockerfile

volumes:
  postgres_catalog:
  postgres_basket: